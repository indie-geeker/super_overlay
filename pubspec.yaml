name: super_overlay
description: Self-managed OverlayEntry dialogs, toasts, popups, loading indicators, and notifications for Flutter.
version: 0.1.1
homepage: https://github.com/indie-geeker/super_overlay
repository: https://github.com/indie-geeker/super_overlay
issue_tracker: https://github.com/indie-geeker/super_overlay/issues

environment:
  sdk: ^3.11.4
  flutter: ">=3.41.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

# For information on the generic Dart part of this file, see the
# following page: https://dart.dev/tools/pub/pubspec

# The following section is specific to Flutter packages.
flutter:
